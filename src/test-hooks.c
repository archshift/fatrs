#include <stddef.h>

#include "ff.h"

size_t fatfs_test_sizeof_BYTE  = sizeof(BYTE);
size_t fatfs_test_sizeof_SHORT = sizeof(SHORT);
size_t fatfs_test_sizeof_WORD  = sizeof(WORD);
size_t fatfs_test_sizeof_TCHAR = sizeof(TCHAR);
size_t fatfs_test_sizeof_INT   = sizeof(INT);
size_t fatfs_test_sizeof_UINT  = sizeof(UINT);
size_t fatfs_test_sizeof_LONG  = sizeof(LONG);
size_t fatfs_test_sizeof_DWORD = sizeof(DWORD);

size_t fatfs_test_sizeof_FATFS = sizeof(FATFS);
size_t fatfs_test_sizeof_FIL   = sizeof(FIL);